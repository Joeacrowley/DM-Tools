---
title: "Data being used"
format: html
editor: visual
---

## Packages

```{r, warnings = F, message = F, echo = T}
library(here)
here::i_am("Data-used.qmd")
library(haven)
library(tidyverse)
library(labelled)
```

## Load BSA teaching data

```{r}
df <- read_sav(here::here("data", "synthetic_bsa2019_sav.sav"), user_na = TRUE)
```

## Variable summary

```{r}
names(df)

df %>% glimpse

look_for(df, details = "none")

look_for(df)
```
